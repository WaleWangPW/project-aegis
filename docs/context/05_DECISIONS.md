# Decisions

Dashboard Contract 2.0 is current.
Evidence before claims; PASS markers alone are not enough.
Verify hashes and exit codes.
No composite score, no fabricated data.
`RecommendationRecord` is core; Risk veto can block Action.
Empty recommendations are valid.
`dashboard/index.html` is never edited by code.

Do not change lightly:
- `deriveDailyDecision`
- contract semantics
- evidence gate rules
- marker naming
