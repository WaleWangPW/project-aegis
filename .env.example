# Project Aegis — environment variables (example only, no real secrets)
# Copy to .env and fill in locally. Never commit the real .env file.

TUSHARE_TOKEN=
AEGIS_DATA_DIR=./data
AEGIS_LOG_LEVEL=INFO
